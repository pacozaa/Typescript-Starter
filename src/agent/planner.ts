import { GenerativeTextInterface } from "../types";

export const planner: GenerativeTextInterface = {
    async generate(input: string) {
        //   const query =  await promptFormat(input);
        //   return await getOllamaStream(query)
        return '';
    }
};